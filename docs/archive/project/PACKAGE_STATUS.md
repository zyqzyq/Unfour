# Package Status

This is the centralized package and crate status matrix for AI/Codex context
loading. Keep current package progress here instead of creating per-package
`PROGRESS.md`, `NEXT_STEPS.md`, or `OPEN_ISSUES.md` files.

This file is the current package/crate status source of truth. Historical
checkpoint docs and architecture references should link here instead of
maintaining competing package status.

| Package | Purpose | Current Status | Stability | Main Risk | Next Gate |
| ------- | ------- | -------------- | --------- | --------- | --------- |
| `apps/desktop` | Tauri desktop composition app that mounts the shell, feature modules, workspace UI, and Tauri adapter. | 发展中 | 部分稳定 | Composition logic is still broad; shell concerns can be confused with feature logic. | Keep feature behavior in packages/crates and move only stable shell composition when explicitly scoped. |
| `packages/app-shell` | Thin frontend shell slot wrapper around shared shell layout primitives. | 部分稳定 | 部分稳定 | Shell layout primitives still live in `packages/ui`; shell orchestration mostly happens in `apps/desktop`. | Keep this package free of API, SSH, DB, and Workspace business logic. |
| `packages/ui` | Shared UI primitives and stateless layout helpers. | 部分稳定 | 部分稳定 | Primitive components and shell layout helpers coexist during the UI split. | Reuse existing primitives first; add new primitives only for cross-module needs. |
| `packages/api-client` | API Debugger frontend: request editor, Send action, response viewer, history, saved requests, collections, environments, import/export. | 发展中 | 部分稳定 | Large feature surface can leak shell, DB, or SSH concerns; request send/history paths must stay aligned with command-client and command bus behavior. | Keep Send as the primary action and route backend calls through `@unfour/command-client`. |
| `packages/ssh-terminal` | SSH Terminal frontend: connections, sessions, terminal panes, split/search/log UI, host-key trust UI. | 发展中 | 待验证 | Real SSH behavior needs live server verification; terminal state and logs touch sensitive data. | Verify password/key auth, PTY I/O, resize, history, host-key trust, and reconnect against a live SSH server. |
| `packages/database` | Database frontend: connection tree, schema browser, SQL editor, query results, table preview, query history. | 发展中 | 有风险 | SQL execution can be destructive if confirmation and safety boundaries drift. | Keep high-risk operations explicit and verify against real SQLite/PostgreSQL/MySQL paths when behavior changes. |
| `packages/command-client` | Typed frontend Tauri command wrappers, shared TypeScript types, and browser mock fallback. | 部分稳定 | 部分稳定 | Browser mocks can drift from Rust command behavior and shared type contracts. | Keep it free of React and feature state; update mocks whenever command contracts change. |
| `packages/workspace-core` | Shared frontend workspace Zustand store and workspace type re-exports. | 部分稳定 | 部分稳定 | Feature packages currently depend on selected resource state as a transitional exception. | Re-evaluate selected connection/request ownership after module split stabilizes. |
| `packages/workspace-local` | Future local workspace lifecycle/persistence boundary; currently re-exports workspace-core for compatibility. | 待验证 | 待验证 | It has no concrete local persistence implementation yet. | Add real local workspace behavior only when explicitly scoped. |
| `crates/unfour-command-bus` | Reusable Rust command entry point for Tauri, MCP, and future AI/CLI adapters. | 部分稳定 | 部分稳定 | Cross-domain methods centralize security-sensitive API, DB, SSH, workspace, and credential paths. | Keep adapters thin and preserve redaction, workspace_id, credential-ref, and confirmation policies. |
| `crates/unfour-mcp` | Local stdio MCP server exposing safe workspace/API/database tools through the command bus. | 发展中 | 部分稳定 | MCP must not bypass command-bus safety, redaction, or read-only database limits. | Keep MCP operations routed through the command bus and verify tool outputs remain sanitized. |
| `crates/unfour-core` | Shared Rust models, error/result types, redaction helpers, and reserved AI/sync contracts. | 部分稳定 | 部分稳定 | Contract changes ripple through all engines, Tauri, command bus, and frontend types. | Change models only with matching adapter/type updates and verification. |
| `crates/local-storage` | SQLite database, migrations, activity log, and local persistence services. | 部分稳定 | 部分稳定 | Migration and activity-log changes can affect persisted user data and secret redaction. | Verify migrations and persisted records whenever schemas change. |
| `crates/http-engine` | API request execution, environment resolution, saved requests, history, and redaction persistence. | 部分稳定 | 部分稳定 | Sensitive request/response data must stay redacted in history and activity paths. | Keep API execution behind command bus and verify redaction on request/history changes. |
| `crates/database-engine` | Database connection CRUD, schema browsing, query execution, browse-table behavior, and SQL safety classification. | 部分稳定 | 有风险 | SQL classification, confirmation, and driver behavior are high-risk. | Run targeted engine tests and live DB checks for behavior changes. |
| `crates/ssh-engine` | SSH connection/session service, terminal events, host-key handling, reconnect, and log export. | 发展中 | 有风险 | Native SSH behavior depends on platform, network, host-key, and credential conditions. | Keep `ssh-native` checks green and perform live SSH verification for terminal changes. |
| `crates/workspace-engine` | Workspace CRUD, active workspace state, environment variables, and layout persistence. | 部分稳定 | 部分稳定 | Workspace identity and layout state are shared across all modules. | Ensure every persisted business record remains workspace-scoped. |
| `crates/secret-store` | Credential reference management backed by OS keychain or in-memory test store. | 部分稳定 | 有风险 | Platform keychain behavior varies; secrets must never fall back to SQLite plaintext. | Runtime-verify platform keychain behavior when credential paths change. |
| `extension-contracts` | No package or crate exists in this checkout; it is not a current workspace member. | 待确认 | 待确认 | Future extension contract scope is not represented in the repository yet. | Confirm whether this package is still planned before creating it. |
